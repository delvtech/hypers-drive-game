{
  "name": "hypers-drive-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "format": "prettier --write 'src/**/*.{js,jsx,ts,tsx,json,md,yaml,yml}'",
    "format:check": "prettier --check '**/*.{js,jsx,ts,tsx,json,md}'"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  },
  "dependencies": {
    "bezier-easing": "^2.1.0",
    "kaboom": "next",
    "prettier": "^2.8.3"
  }
}
